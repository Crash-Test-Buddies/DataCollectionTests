<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rit.se.crashavoidance.datacollectiontests">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- MainActivity -->
        <activity
            android:name=".views.MainActivity"
            android:screenOrientation="portrait" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Wi-Fi Buddy -->
        <service android:name="edu.rit.se.wifibuddy.WifiDirectHandler" />

        <service android:name="crashavoidance.se.rit.datacollector.service.DBHandlerService"
            android:exported="false" android:process=":services" >
            <intent-filter>
                <!-- Start/Stop service -->
                <action android:name="crashavoidance.se.rit.datacollector.service.START_SERVICE" />
            </intent-filter>
        </service>

    </application>

</manifest>
